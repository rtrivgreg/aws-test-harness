# Copy to config/settings.yaml and adjust.
# The harness currently prefers environment variables; this file is
# reserved for future richer configuration.

aws_region: us-east-1

catalog:
  table_name: "your-config-rules-catalog-table"
  group: "default"          # organizational binding used by cpgNG.py

# Optional safety – currently not enforced (per user decision)
# allowed_account_ids: []
