# Session 2026-08-29 — backend confirm + park RP encrypted + scratchpad

Account `418295699841` us-east-1. Runner `ip-10-0-1-190`.
Run id `ef57dcf4`.

## Backend

- tfstate bucket versioning Enabled
- live test bucket versioning Suspended (toggle leftover)
- lock table ACTIVE
- extra 14ac09fc buckets still listed

## BACKUP_RECOVERY_POINT_ENCRYPTED — parked

Pytest created two EBS volumes, vault `cfg-rp-vault-ef57dcf4`, two COMPLETED jobs:

| Job | Volume | RP |
|---|---|---|
| 6be2a7fd-389a-438a-8999-4c50539e745a | vol-0d2fc1f68ad84617f | snap-0ed309ee3a0da0752 |
| c4e8735b-2bb6-41c5-ab1c-fcc36a1427a4 | vol-083f9e5a4a3c260ba | snap-023c1211f37c3fb71 |

`list-discovered-resources AWS::Backup::RecoveryPoint` = `[]`.

Role cannot `DescribeBackupVault` / `ListRecoveryPointsByBackupVault`.
`list-backup-jobs` works. Tag filters on volumes/snapshots returned empty
(Backup snapshots do not carry harness tags).

JMESPath note: `contains(resourceType, \`Backup\`)` is eaten by bash.
Use single quotes: `'resourceCounts[?contains(resourceType, `Backup`)]'`.

## Decision

Park. Same recorder gap as FSx. Do not rerun RP-typed rules here.

## Afternoon — root disk + scratchpad

t3.medium: ~40G root, ~20G free-ish on extra volume.

```
nvme0n1p16  /boot  913M
nvme1n1     /mnt/scratchpad  20G
```

Created:

- `/mnt/scratchpad/terraform/plugin_cache`
- `/mnt/scratchpad/terraform/tmp`
- `/mnt/scratchpad/pytest_tmp/.pytest_cache`

Exports persisted in `~/.bashrc`:

```bash
export TMPDIR="/mnt/scratchpad/pytest_tmp"
export TF_PLUGIN_CACHE_DIR="/mnt/scratchpad/terraform/plugin_cache"
export TF_DATA_DIR="/mnt/scratchpad/terraform/.terraform"
```

Pytest cache flag: `--cache-dir=/mnt/scratchpad/pytest_tmp/.pytest_cache`.
No live rule cycle after this mount in this session.
