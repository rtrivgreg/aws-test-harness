# Session 2026-08-30 — resume + S3 Express candidate

Account `418295699841` us-east-1. Runner `ip-10-0-1-190`.
Run id `ef57dcf4`.

## Start-of-day

Last lock (2026-08-29 17:49 EDT): `S3_LAST_BACKUP_RECOVERY_POINT_CREATED`.
Score **38 / 59**. README and matrix were still at 33/34; synced this morning.

Do not apply Terraform. `S3_TEST_BUCKET=cfg-test-ef57dcf4-ca589695`.

## Next rule

`S3EXPRESS_DIR_BUCKET_LIFECYCLE_RULES_CHECK`

- Resource: `AWS::S3Express::DirectoryBucket`
- Trigger: configuration change
- Optional param: `targetExpirationDays`
- Test creates a throwaway directory bucket (no lifecycle = NC),
  then PutBucketLifecycleConfiguration ExpirationInDays=7 (C).
- Control-plane endpoint: `https://s3express-control.us-east-1.amazonaws.com`
- AZ fallback: use1-az4, use1-az5, use1-az6
- If discovery times out, park the same way as FSx. Delete the bucket either way.

```bash
cd ~/repost/aws-test-harness
git pull
source .venv/bin/activate
pytest tests/test_s3express_lifecycle_rules.py -q -s \
  -o cache_dir=/mnt/scratchpad/pytest_tmp/.pytest_cache
```
