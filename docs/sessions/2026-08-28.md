# Session 2026-08-28 — live proofs + Terraform backend

Account: `418295699841` (us-east-1). Runner: Ubuntu EC2 `ip-10-0-1-190`.
Git run id on live resources: `ef57dcf4`.
Config recorder: `EXCLUSION_BY_RESOURCE_TYPES` (IAM types excluded), recording SUCCESS.

## Score at freeze

22 / 26 live proofs = 85%. Session grade A-.

## Locked this day (or confirmed on this box)

- S3_BUCKET_ACL_PROHIBITED — public-read ACL NC, BucketOwnerEnforced C
  (`cfg-test-ef57dcf4-ca589695`)
- VPC_SG_PORT_RESTRICTION_CHECK — 3389 open/close on `sg-07fa913d0e8961833`
- S3_DEFAULT_ENCRYPTION_KMS — AES256 NC, aws:kms C
- CLOUD_TRAIL_ENCRYPTION_ENABLED — `alias/harness`
  `arn:aws:kms:us-east-1:418295699841:key/45b99a45-cb78-47e5-9f70-0296ef21bee7`
  trail `cfg-ct-ef57dcf4`

Already locked from prior sessions and still standing: S3 versioning,
lifecycle, version-lifecycle, public-access, SSL, logging, public-read,
public-write; EBS encrypted volumes; EFS encrypted/backups/APs;
CloudTrail log-file validation; EC2 IMDSv2 + restricted SSH; Backup min
frequency/retention.

## Parked with forensics

### RESTRICTED_INCOMING_TRAFFIC (restricted-common-ports)

Four live pytest misses, then a 45s probe `harness-forensic-rcp`:

- Rule ACTIVE, DETECTIVE, scope AWS::EC2::SecurityGroup, blockedPorts=3389
- LastSuccessfulInvocationTime set, FirstEvaluationStarted=true
- No LastSuccessfulEvaluationTime, no LastError*
- describe-compliance-by-config-rule = INSUFFICIENT_DATA
- get-compliance-details-by-config-rule = []
- get-compliance-details-by-resource on the test SG = []
- SG CI exists (OK)

Replacement lock: VPC_SG_PORT_RESTRICTION_CHECK. Do not rerun RCP.

### FSx OpenZFS / FSX_OPENZFS_COPY_TAGS_ENABLED

- `fs-0e541e6d001fad3d9` Lifecycle=AVAILABLE after create
- Config GetResourceConfigHistory + list-discovered-resources empty
  after 26 minutes
- Targeted destroy completed (10m1s). describe-file-systems = []

### S3_BUCKET_SERVER_SIDE_ENCRYPTION_ENABLED

Not run as NC. Modern S3 default encryption makes delete_bucket_encryption
unable to prove NON_COMPLIANT. Use S3_DEFAULT_ENCRYPTION_KMS instead.

### EBS_SNAPSHOT_PUBLIC_RESTORABLE_CHECK

NC path previously proven. COMPLIANT is account-scoped xfail.

## Terraform backend migrate

Local `terraform/terraform.tfstate` copied to:

- bucket `tfstate-aws-test-harness-418295699841`
- key `aws-test-harness/terraform.tfstate`
- lock table `tfstate-aws-test-harness-lock` (id `860c7062-08b5-4029-8737-00e3cb12f213`)

`terraform init -migrate-state` → yes.

Plan without TF_VAR_enable_cloudtrail_test=true proposes 5 destroys
(module count 0). Plan with the flag proposes 1 in-place change (strip
KMS + harness tags). Neither applied.

Terraform stays on this EC2 only.

## Operating rules learned

- pytest from repo root, not terraform/
- One live pytest at a time
- Do not apply Terraform just to clear toggle drift
- Do not leave FSx up after a discovery miss
- Mac: git/CLI only until a dedicated new account exists
